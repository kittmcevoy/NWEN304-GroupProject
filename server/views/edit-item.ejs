<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <title>Add Item</title>
    <link rel="stylesheet" href="/css/main.css" />
    <link rel="stylesheet" href="/css/item.css" />
    <link rel="stylesheet" type="text/css" href="/css/style.css" />
    <link
            rel="stylesheet"
            href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css"
    />
</head>

<body>
<%- include('./navbar.ejs', { user: user}); %>

<main>
    <form
            class="product-form"
            action="/edit-item/<%= item._id %>"
            method="POST"
            enctype="multipart/form-data"
    >
        <div class="form-control">
            <label for="title">Title</label>
            <input type="text" name="title" id="" value="<%= item.title %>"/>
        </div>
        <div class="form-control">
            <label for="desc">Description</label>
            <input type="string" name="desc" id="" value="<%= item.desc %>"/>
        </div>
        <div class="form-control">
            <label for="size">Size</label>
            <input type="text" name="size" id="" value="<%= item.size %>"/>
        </div>
        <div class="form-control">
            <label for="color">Color</label>
            <input type="text" name="color" id="" value="<%= item.color %>"/>
        </div>
        <div class="form-control">
            <label for="price">Price</label>
            <input type="number" name="price" id="" step="any" value="<%= item.price %>"/>
        </div>

        <div class="form-control">
            <label for="url">Image URL</label>
            <input type="text" name="url" id="url" placeholder="image.png" step="any" value="<%= item.url %>"/>
        </div>
        <div class="form-control">
            <label for="image">Image</label>
            <input
                    type="file"
                    id="image"
                    name="image"
                    accept="image/jpeg, image/png, image/jpg"
                    value="<%= item.image %>"
            />
            <img src="<%= item.image %>" , alt="Item to add" />
        </div>

        <button
                type="submit"
                style="color: black; font-size: 18px; padding: 5px 5px"
        >
            Update Item
        </button>
    </form>
</main>
</body>
</html>
